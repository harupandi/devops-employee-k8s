# Dev environment overrides.
# Deploy with:
#   helm upgrade --install devops-employee-dev ./charts/devops-employee \
#     -f ./charts/devops-employee/values.yaml \
#     -f ./charts/devops-employee/values-dev.yaml \
#     -n dev --create-namespace
#
# These are the exact image tags currently hardcoded in apps/backend/deployment.yaml
# and apps/frontend/deployment.yaml - carried over as-is so the migration is a
# like-for-like swap. Once your CI/CD updates dev automatically, this tag will be
# overridden per-deploy with --set or by whatever writes to this file (see the
# CI notes at the end of the migration guide).

backend:
  image:
    tag: "9276d00a2fda75411c2228948151cdacab1ad672"
frontend:
  image:
    tag: "e609ff40b62c3184b55da8d9dfe224e4c927f1d3"
