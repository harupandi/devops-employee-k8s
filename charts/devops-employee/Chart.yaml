apiVersion: v2
name: devops-employee
description: Helm chart for the devops-employee backend/frontend application on AKS
type: application

# Chart version - bump this whenever templates/values.yaml structure changes.
version: 0.1.0

# App version - purely informational (shown in `helm list`), doesn't drive image tags.
appVersion: "1.0.0"

keywords:
  - devops-employee
  - backend
  - frontend
home: https://github.com/harupandi/devops-employee-k8s
